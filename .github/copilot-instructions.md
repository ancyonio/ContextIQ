# Copilot instructions for this repo

This project provides **ContextIQ**, a local code-graph MCP server (registered
under the id `tokengraph`), for token-efficient context. In agent mode, **prefer
its tools over reading whole files**.

## Use the graph first

1. **`find_relevant_context(task)`** — default. A token-budgeted pack of the most
   relevant symbols (core bodies, neighbor signatures, indexed chunks, module
   summaries). Read this instead of opening many files.
2. **`search_semantic(query)`** — find symbols by meaning when the exact name is
   unknown.
3. **`get_symbol(qname)`** — full source of one symbol.
4. **`get_callers(qname)` / `get_callees(qname)`** — call-graph navigation.
5. **`get_module_summary(file)`** — a few-token overview of a file.
6. **`file_skeleton(file)`** — all signatures in a file, no bodies.
7. **`estimate_savings(task)`** — token savings vs. reading files whole.

Open a file in full only when the pack drops something you still need, or when
you must edit it.

## Freshness

The graph auto-refreshes on every tool call (changed files are reparsed before
answering), so results are never stale. Copilot has no edit hook, so if you want
continuous pre-warming run `python tokengraph_all.py watch` alongside your
session; otherwise the per-call refresh already keeps it correct.

## Setup

The MCP server is configured in `.vscode/mcp.json`. Start it from that file
(VS Code 1.102+, agent mode) or reload the window. It needs `pip install fastmcp`.

<!-- tokengraph:begin (generated — do not edit) -->
<!-- tokengraph:source e6393beb650dd79613dbf2a17130ea76 -->
# Project context — signature map (generated by tokengraph)
_Strategy `hot-cold` · 43 files · signatures ≈13,154 tokens vs ~269,092 full-source._

Compact signatures only. For full bodies use the `tokengraph` MCP server: `list_modules()` → `read_context(module=…)` / `query_context(task)` / `get_symbol(qname)` / `get_lines(file,start,end)`.

## Modules
| module | files | tokens | symbols |
|---|--:|--:|--:|
| . | 4 | 175,491 | 949 |
| tests | 3 | 37,949 | 425 |
| benchmarks | 26 | 37,480 | 713 |
| .github | 3 | 8,883 | 174 |
| docs | 2 | 7,461 | 57 |
| tools | 1 | 1,431 | 11 |
| .prompts | 4 | 397 | 20 |

## Hot — recently changed (full signatures injected)
### .github/copilot-instructions.md
```
# skeleton: .github/copilot-instructions.md
# Copilot instructions for this repo
## Use the graph first
## Freshness
## Setup
# Project context — signature map (generated by tokengraph)
## Modules
## Hot — recently changed (full signatures injected)
### README.md    # # Hot — recently changed (full signatures injected)
# skeleton: README.md
# <p align="center"> <img src="images/ContextIQ_Inline.png" align ="center" width="400" alt="ContextIQ"> </p>    # skeleton: README.md
# ContextIQ    # skeleton: README.md
## Why it saves tokens    # skeleton: README.md
## Install    # skeleton: README.md
## CLI usage    # skeleton: README.md
# Build / update the local graph    # skeleton: README.md
# Get a token-budgeted context pack for a task (prints markdown)    # skeleton: README.md
# Other lookups    # skeleton: README.md
# Orientation, impact, and surgical fetch    # skeleton: README.md
# Decision support, gating, and cost control    # skeleton: README.md
# Cost, prompt quality, dedup, and session compression    # skeleton: README.md
# Grounded creation: retrieval -> safe code generation    # skeleton: README.md
# Auditable evidence + guard measurement    # skeleton: README.md
# Cross-session memory (local to the repo)    # skeleton: README.md
# Multi-assistant export + distribution + editor integration    # skeleton: README.md
### `context` options    # skeleton: README.md
# … +60 more symbol(s) (capped at 25/file; fetch via get_symbol/search)
```

### .github/workflows/ci.yml
```yaml
# skeleton: .github/workflows/ci.yml
name: CI
on:
pull_request:
push:
branches: [main]
schedule:    # The LLM-judged evaluation below costs real API spend, so it cannot run on every push — but "too expensive for every PR" had quietly become "never runs at all", leaving the project's central claim unte…
workflow_dispatch:
concurrency:
group: ci-${{ github.ref }}
cancel-in-progress: true
env:
TOKENGRAPH_EMBEDDINGS: "off"    # Keep CI on the deterministic hash backend: identical scores on every runner, no model download, no network dependency in the gate.
TOKENGRAPH_NO_TRACK: "1"
jobs:
test:
name: tests (py${{ matrix.python }} / ${{ matrix.os }})
runs-on: ${{ matrix.os }}
strategy:
fail-fast: false
matrix:
os: [ubuntu-latest, windows-latest]
python: ['3.10', '3.12']
steps:
with:
python-version: ${{ matrix.python }}
# … +45 more symbol(s) (capped at 25/file; fetch via get_symbol/search)
```

### tests/test_contextiq_all.py
```python
# skeleton: tests/test_contextiq_all.py
_PYPROJECT = Path(tg.__file__).resolve().parent / "pyproject.toml"
def _write(root: Path, rel: str, text: str) -> Path
SAMPLE = '''\ def helper(x): """Double a number.""" return x * 2 class Greeter: def greet(self, name): return helper(len(name)) '''
class IndexerTests(unittest.TestCase)
    def setUp(self)
    def tearDown(self)
    def test_index_extracts_symbols_and_edges(self)
    def test_incremental_fast_path_skips_unchanged(self)
    def test_touched_mtime_same_content_does_not_reparse(self)
    def test_change_is_reindexed(self)
    def test_deleted_file_is_forgotten(self)
    def test_freshen_keeps_symbol_bodies_aligned_after_edit(self)    # The core correctness guarantee: after lines shift, a re-index must
    def test_incremental_edit_preserves_unchanged_cross_file_caller(self)
    def test_targeted_index_only_scans_notified_path(self)
    def test_import_scip_adds_precise_reference_edge(self)
    def test_gitignore_excludes_files_and_dirs(self)
    def test_context_pack_respects_budget(self)
    def test_embeddings_and_semantic_search(self)
    def test_defines_edges(self)
    def test_module_summary_generated_and_cacheable(self)
    def test_measure_reports_savings(self)
    def test_report_aggregates_savings(self)
    def test_report_append_accumulates_across_runs(self)    # `report --append` keeps one growing sheet instead of overwriting.
    def test_report_append_writes_header_into_an_empty_file(self)    # A pre-created but empty file must still get the header.
    def test_import_aware_resolution(self)
# … +364 more symbol(s) (capped at 25/file; fetch via get_symbol/search)
```

### tokengraph_all.py
```python
# skeleton: tokengraph_all.py
_enc = None
_tried = False
def _encoder()
_MODEL_ENCODERS: dict[str, object] = {}
_MODEL_ENCODER_TRIED: set[str] = set()
_TIKTOKEN_ENCODINGS = { "o200k": ("gpt-4o", "gpt-4.1", "gpt-5", "o1", "o3", "o4"), }
def _tiktoken_encoding_for(model: str)    # Exact tiktoken encoding for an OpenAI model, else None.
def _hf_tokenizer(repo_id: str)    # Load a local HuggingFace tokenizer without touching the network.
def _model_encoder(model: str)    # Return (encoder, kind) for a model. kind is 'exact' or 'approx'.
def _encode_len(enc, text: str) -> int    # Length of `text` under either a tiktoken or a HuggingFace tokenizer.
def count_tokens(text: str) -> int
MODEL_TOKEN_RATIOS: dict[str, float] = { "gpt": 1.00, "claude": 1.00, "gemini": 0.98, "llama": 1.10, }
def model_family(model: str) -> str    # Map a concrete model name to a tokenizer/pricing family.
def count_tokens_for_model(text: str, model: str = "gpt-4o") -> int    # Token count for a specific model (MA-2).
def count_tokens_detail(text: str, model: str = "gpt-4o") -> dict    # Model token count plus provenance: exact tokenizer vs. estimate.
_APPROX_NOTE = { "claude": ("Anthropic publishes no offline tokenizer, so Claude counts are " "a calibrated estimate. Treat as ±5% for budgeting."), "gemini": ("Estimate. For exact counts install `transformers` and cache a " "Gemma tokenize …
def count_tokens_claude_api(text: str, model: str = "claude-opus-4-8") -> dict    # Exact Claude token count via the Anthropic count_tokens endpoint (MA-3).
def tokenizer_status() -> dict    # Which families can be counted exactly on this machine, and which cannot.
MAX_SIGS_PER_FILE = 25
SEED_CANDIDATES = 20
MAX_FANOUT_PER_SYMBOL = 150 # neighbours collected from any single symbol
MAX_NEIGHBOR_CANDIDATES = 600 # global ceiling on collected candidates
MAX_NEIGHBOR_SIGS = 40 # neighbour signatures actually emitted
NEIGHBOR_REASON_WEIGHT = {"callee": 1.0, "base": 0.95, "caller": 0.7}
SWEEP_FILES = 4 # files eligible for completion, by file score
# … +858 more symbol(s) (capped at 25/file; fetch via get_symbol/search)
```

### README.md
```
# skeleton: README.md
# <p align="center"> <img src="images/ContextIQ_Inline.png" align ="center" width="400" alt="ContextIQ"> </p>
# ContextIQ
## Why it saves tokens
## Model-agnostic — works with any LLM, including local models (no API key)
## Install
## CLI usage
# Build / update the local graph
# Get a token-budgeted context pack for a task (prints markdown)
# Other lookups
# Orientation, impact, and surgical fetch
# Decision support, gating, and cost control
# Cost, prompt quality, dedup, and session compression
# Grounded creation: retrieval -> safe code generation
# Auditable evidence + guard measurement
# Cross-session memory (local to the repo)
# Multi-assistant export + distribution + editor integration
### `context` options
## Semantic search (embeddings)
## Staying fresh (no stale mapping)
## Proving the savings: the ledger, `gain`, and the dashboard
### The per-workspace dashboard: `.tokengraph/token-usage.html`
# branch=main  dirty=3  indexed=15 files / 676 symbols  index=1m ago
# notes=2  savings=209,426 tok over 2 run(s)    # branch=main  dirty=3  indexed=15 files / 676 symbols  index=1m ago
# report=/path/to/repo/.tokengraph/token-usage.html    # branch=main  dirty=3  indexed=15 files / 676 symbols  index=1m ago notes=2  savings=209,426 tok over 2 run(s)
## Working across a monorepo
# … +26 more symbol(s) (capped at 25/file; fetch via get_symbol/search)
```

### benchmarks/repos/gosvc/cmd/gosvc/main.go
```go
# skeleton: benchmarks/repos/gosvc/cmd/gosvc/main.go
const ShutdownGrace = 20 * time.Second    # ShutdownGrace bounds how long we wait for the loops to finish.
dependencies struct { cfg *config.Config log *logging.Logger pool *pool.Pool breaker *breaker.Breaker store *store.Store service *server.Service consumer *consumer.Consumer }
cfg *config.Config
log *logging.Logger
pool *pool.Pool
breaker *breaker.Breaker
store *store.Store
service *server.Service
consumer *consumer.Consumer
func build(cfg *config.Config, dial pool.Dialer, reader consumer.Reader, dead consumer.DeadLetterWriter) *dependencies    # build assembles the object graph from a validated configuration.
func startMaintenance(ctx context.Context, deps *dependencies, wg *sync.WaitGroup)    # startMaintenance runs the periodic pool sweep until the context is done.
func startConsumer(ctx context.Context, deps *dependencies, wg *sync.WaitGroup)    # startConsumer runs the event loop in its own goroutine.
func awaitSignal(ctx context.Context)    # awaitSignal blocks until the process is asked to stop.
func run(dial pool.Dialer, reader consumer.Reader, dead consumer.DeadLetterWriter) int    # run is main's testable body: it returns an exit code instead of exiting.
func main()
var defaultDialer pool.Dialer = func(ctx context.Context) (pool.Conn, error) { return nil, errors.New("no driver linked into this build") }    # defaultDialer opens a connection to the configured store. The real build links this against the database driver; the signature is the contract.
defaultReader struct{}
    func (defaultReader) Fetch(ctx context.Context, max int) ([]consumer.Message, error)    # Fetch returns no records; the real build swaps in the broker client.
    func (defaultReader) Commit(ctx context.Context, offsets map[int32]int64) error    # Commit acknowledges offsets back to the broker.
    func (defaultReader) Close() error    # Close releases the broker connection.
defaultDeadLetter struct{}
    func (defaultDeadLetter) Publish(ctx context.Context, msg consumer.Message, reason string) error    # Publish records that a message was abandoned.
```

### benchmarks/repos/gosvc/internal/breaker/breaker.go
```go
# skeleton: benchmarks/repos/gosvc/internal/breaker/breaker.go
State int
const ( StateClosed State = iota StateOpen StateHalfOpen )    # The three states, in the order they are normally visited.
const ( StateClosed State = iota StateOpen StateHalfOpen )    # The three states, in the order they are normally visited.
const ( StateClosed State = iota StateOpen StateHalfOpen )    # The three states, in the order they are normally visited.
const ( BucketCount = 10 BucketDuration = 6 * time.Second MinimumSamples = 20 HalfOpenProbes = 3 )    # Window shape: ten buckets covering one minute.
const ( BucketCount = 10 BucketDuration = 6 * time.Second MinimumSamples = 20 HalfOpenProbes = 3 )    # Window shape: ten buckets covering one minute.
const ( BucketCount = 10 BucketDuration = 6 * time.Second MinimumSamples = 20 HalfOpenProbes = 3 )    # Window shape: ten buckets covering one minute.
const ( BucketCount = 10 BucketDuration = 6 * time.Second MinimumSamples = 20 HalfOpenProbes = 3 )    # Window shape: ten buckets covering one minute.
var ErrOpen = errors.New("circuit is open; call was not attempted")    # ErrOpen is returned instead of calling through when the breaker is open.
    func (s State) String() string    # String renders a State for logs and the health endpoint.
bucket struct { startedAt time.Time successes int failures int }
startedAt time.Time
successes int
failures int
Snapshot struct { State string Failures int Successes int FailureRatio float64 OpenedAt time.Time }
State string
Failures int
Successes int
FailureRatio float64
OpenedAt time.Time
Breaker struct { mu sync.Mutex buckets [BucketCount]bucket cursor int state State openedAt time.Time probesLeft int cooldown time.Duration failureRatio float64 clock func() time.Time }
mu sync.Mutex
buckets [BucketCount]bucket
cursor int
state State
# … +13 more symbol(s) (capped at 25/file; fetch via get_symbol/search)
```

### benchmarks/repos/gosvc/internal/config/config.go
```go
# skeleton: benchmarks/repos/gosvc/internal/config/config.go
const ( DefaultListenAddr = ":9090" DefaultPoolSize = 16 DefaultDialTimeout = 3 * time.Second DefaultRequestTimeout = 10 * time.Second DefaultConsumerGroup = "gosvc-workers" DefaultCommitInterval = 5 * time.Second DefaultBreakerCooldown = 3    # Defaults applied when the corresponding variable is unset.
const ( DefaultListenAddr = ":9090" DefaultPoolSize = 16 DefaultDialTimeout = 3 * time.Second DefaultRequestTimeout = 10 * time.Second DefaultConsumerGroup = "gosvc-workers" DefaultCommitInterval = 5 * time.Second DefaultBreakerCooldown = 3    # Defaults applied when the corresponding variable is unset.
const ( DefaultListenAddr = ":9090" DefaultPoolSize = 16 DefaultDialTimeout = 3 * time.Second DefaultRequestTimeout = 10 * time.Second DefaultConsumerGroup = "gosvc-workers" DefaultCommitInterval = 5 * time.Second DefaultBreakerCooldown = 3    # Defaults applied when the corresponding variable is unset.
const ( DefaultListenAddr = ":9090" DefaultPoolSize = 16 DefaultDialTimeout = 3 * time.Second DefaultRequestTimeout = 10 * time.Second DefaultConsumerGroup = "gosvc-workers" DefaultCommitInterval = 5 * time.Second DefaultBreakerCooldown = 3    # Defaults applied when the corresponding variable is unset.
const ( DefaultListenAddr = ":9090" DefaultPoolSize = 16 DefaultDialTimeout = 3 * time.Second DefaultRequestTimeout = 10 * time.Second DefaultConsumerGroup = "gosvc-workers" DefaultCommitInterval = 5 * time.Second DefaultBreakerCooldown = 3    # Defaults applied when the corresponding variable is unset.
const ( DefaultListenAddr = ":9090" DefaultPoolSize = 16 DefaultDialTimeout = 3 * time.Second DefaultRequestTimeout = 10 * time.Second DefaultConsumerGroup = "gosvc-workers" DefaultCommitInterval = 5 * time.Second DefaultBreakerCooldown = 3    # Defaults applied when the corresponding variable is unset.
const ( DefaultListenAddr = ":9090" DefaultPoolSize = 16 DefaultDialTimeout = 3 * time.Second DefaultRequestTimeout = 10 * time.Second DefaultConsumerGroup = "gosvc-workers" DefaultCommitInterval = 5 * time.Second DefaultBreakerCooldown = 3    # Defaults applied when the corresponding variable is unset.
var ErrMissing = errors.New("required environment variable is not set")    # ErrMissing is returned when a mandatory variable has not been supplied.
Config struct { Env string ListenAddr string ServiceName string LogLevel string DatabaseDSN string PoolSize int DialTimeout time.Duration RequestTimeout time.Duration BreakerCooldown time.Duration Fai
Env string
ListenAddr string
ServiceName string
LogLevel string
DatabaseDSN string
PoolSize int
DialTimeout time.Duration
RequestTimeout time.Duration
BreakerCooldown time.Duration
FailureRatio float64
Brokers []string
Topic string
ConsumerGroup string
CommitInterval time.Duration
    func (c *Config) Validate() error    # Validate reports the first inconsistency it finds in an assembled Config.
    func (c *Config) Redacted() map[string]any    # Redacted returns a map safe to write to logs: the DSN password is removed.
# … +8 more symbol(s) (capped at 25/file; fetch via get_symbol/search)
```

### benchmarks/repos/gosvc/internal/consumer/consumer.go
```go
# skeleton: benchmarks/repos/gosvc/internal/consumer/consumer.go
const ( MaxPollRecords = 200 RetryBaseDelay = 200 * time.Millisecond RetryMaxDelay = 10 * time.Second MaxDeliveryTries = 5 )    # Loop tuning.
const ( MaxPollRecords = 200 RetryBaseDelay = 200 * time.Millisecond RetryMaxDelay = 10 * time.Second MaxDeliveryTries = 5 )    # Loop tuning.
const ( MaxPollRecords = 200 RetryBaseDelay = 200 * time.Millisecond RetryMaxDelay = 10 * time.Second MaxDeliveryTries = 5 )    # Loop tuning.
const ( MaxPollRecords = 200 RetryBaseDelay = 200 * time.Millisecond RetryMaxDelay = 10 * time.Second MaxDeliveryTries = 5 )    # Loop tuning.
var ErrPoisonMessage = errors.New("message cannot be processed")    # ErrPoisonMessage marks a record that will never succeed and should be routed to the dead-letter topic instead of retried forever.
Message struct { Topic string Partition int32 Offset int64 Key []byte Value []byte Timestamp time.Time Tries int }
Topic string
Partition int32
Offset int64
Key []byte
Value []byte
Timestamp time.Time
Tries int
Reader interface { Fetch(ctx context.Context, max int) ([]Message, error) Commit(ctx context.Context, offsets map[int32]int64) error Close() error }
DeadLetterWriter interface { Publish(ctx context.Context, msg Message, reason string) error }
shipmentEvent struct { ShipmentID string `json:"shipment_id"` OrderID string `json:"order_id"` Carrier string `json:"carrier"` Status string `json:"status"` Weight float64 `json:"weight"` Version int6
ShipmentID string `json:"shipment_id"`
OrderID string `json:"order_id"`
Carrier string `json:"carrier"`
Status string `json:"status"`
Weight float64 `json:"weight"`
Version int64 `json:"version"`
Metrics struct { Fetched int64 Processed int64 Retried int64 DeadLetter int64 Commits int64 }
Fetched int64
Processed int64
# … +22 more symbol(s) (capped at 25/file; fetch via get_symbol/search)
```

### benchmarks/repos/gosvc/internal/logging/logger.go
```go
# skeleton: benchmarks/repos/gosvc/internal/logging/logger.go
Level int
const ( LevelDebug Level = iota LevelInfo LevelWarn LevelError )    # The severities the service emits, in increasing order of urgency.
const ( LevelDebug Level = iota LevelInfo LevelWarn LevelError )    # The severities the service emits, in increasing order of urgency.
const ( LevelDebug Level = iota LevelInfo LevelWarn LevelError )    # The severities the service emits, in increasing order of urgency.
const ( LevelDebug Level = iota LevelInfo LevelWarn LevelError )    # The severities the service emits, in increasing order of urgency.
const TimestampKey = "ts"    # TimestampKey is the field name every record uses for its clock reading.
Fields map[string]any
    func (l Level) String() string    # String renders a Level the way it appears in the emitted JSON.
func ParseLevel(name string) Level    # ParseLevel maps a configuration string onto a Level, defaulting to info.
Logger struct { mu sync.Mutex out io.Writer minLevel Level base Fields clock func() time.Time }
mu sync.Mutex
out io.Writer
minLevel Level
base Fields
clock func() time.Time
func New(out io.Writer, minLevel Level, base Fields) *Logger    # New builds a logger writing to out.
    func (l *Logger) With(extra Fields) *Logger    # With returns a copy of the logger carrying additional permanent fields.
    func (l *Logger) Enabled(level Level) bool    # Enabled reports whether a record at level would be written.
    func (l *Logger) log(level Level, msg string, extra Fields)    # log renders and writes one record, dropping it when the level is too low.
    func (l *Logger) Debug(msg string, extra Fields)    # Debug writes a record at debug level.
    func (l *Logger) Info(msg string, extra Fields)    # Info writes a record at info level.
    func (l *Logger) Warn(msg string, extra Fields)    # Warn writes a record at warn level.
    func (l *Logger) Error(msg string, err error, extra Fields)    # Error writes a record at error level, attaching err when it is non-nil.
    func (l *Logger) FieldNames() []string    # FieldNames lists the permanent field names, sorted, for diagnostics.
```

### benchmarks/repos/gosvc/internal/pool/pool.go
```go
# skeleton: benchmarks/repos/gosvc/internal/pool/pool.go
const ( MaxIdleTime = 5 * time.Minute HealthCheckEvery = 30 * time.Second acquireSpinGuard = 250 * time.Millisecond )    # Tunables that are not worth putting in the config file.
const ( MaxIdleTime = 5 * time.Minute HealthCheckEvery = 30 * time.Second acquireSpinGuard = 250 * time.Millisecond )    # Tunables that are not worth putting in the config file.
const ( MaxIdleTime = 5 * time.Minute HealthCheckEvery = 30 * time.Second acquireSpinGuard = 250 * time.Millisecond )    # Tunables that are not worth putting in the config file.
var ErrPoolClosed = errors.New("pool is closed")    # ErrPoolClosed is returned once Close has been called.
var ErrAcquireTimeout = errors.New("timed out waiting for a connection")    # ErrAcquireTimeout is returned when the caller's context expired while waiting for a free connection.
Conn interface { Ping(ctx context.Context) error Close() error ID() string }
Dialer func(ctx context.Context) (Conn, error)
entry struct { conn Conn lastUsed time.Time uses int64 }
conn Conn
lastUsed time.Time
uses int64
Stats struct { Size int Idle int InUse int Dialed int64 Discarded int64 Waited int64 }
Size int
Idle int
InUse int
Dialed int64
Discarded int64
Waited int64
Pool struct { mu sync.Mutex idle []*entry dialer Dialer size int inUse int closed bool dialed int64 discarded int64 waited int64 slots chan struct{} clock func() time.Time }
mu sync.Mutex
idle []*entry
dialer Dialer
size int
inUse int
closed bool
# … +13 more symbol(s) (capped at 25/file; fetch via get_symbol/search)
```

### benchmarks/repos/gosvc/internal/server/handlers.go
```go
# skeleton: benchmarks/repos/gosvc/internal/server/handlers.go
Code int
const ( CodeOK Code = iota CodeInvalidArgument CodeNotFound CodeAborted CodeUnavailable CodeInternal )    # The subset of status codes the handlers use.
const ( CodeOK Code = iota CodeInvalidArgument CodeNotFound CodeAborted CodeUnavailable CodeInternal )    # The subset of status codes the handlers use.
const ( CodeOK Code = iota CodeInvalidArgument CodeNotFound CodeAborted CodeUnavailable CodeInternal )    # The subset of status codes the handlers use.
const ( CodeOK Code = iota CodeInvalidArgument CodeNotFound CodeAborted CodeUnavailable CodeInternal )    # The subset of status codes the handlers use.
const ( CodeOK Code = iota CodeInvalidArgument CodeNotFound CodeAborted CodeUnavailable CodeInternal )    # The subset of status codes the handlers use.
const ( CodeOK Code = iota CodeInvalidArgument CodeNotFound CodeAborted CodeUnavailable CodeInternal )    # The subset of status codes the handlers use.
const MaxTrackingIDLength = 64    # MaxTrackingIDLength bounds the identifier a caller may send.
Status struct { Code Code Message string Details map[string]string }
Code Code
Message string
Details map[string]string
    func (s *Status) Error() string    # Error makes Status satisfy the error interface.
GetShipmentRequest struct { ShipmentID string TraceID string }
ShipmentID string
TraceID string
GetShipmentResponse struct { ShipmentID string OrderID string Carrier string Status string Version int64 UpdatedAt time.Time }
ShipmentID string
OrderID string
Carrier string
Status string
Version int64
UpdatedAt time.Time
AdvanceShipmentRequest struct { ShipmentID string NextStatus string FromVersion int64 TraceID string }
ShipmentID string
# … +21 more symbol(s) (capped at 25/file; fetch via get_symbol/search)
```

### benchmarks/repos/gosvc/internal/store/store.go
```go
# skeleton: benchmarks/repos/gosvc/internal/store/store.go
var ErrNotFound = errors.New("record not found")    # ErrNotFound is returned when a lookup by key matches nothing.
var ErrConflict = errors.New("record was modified concurrently")    # ErrConflict is returned when an optimistic update loses the race.
const MaxBatchSize = 500    # MaxBatchSize caps how many records one write may carry.
Shipment struct { ID string OrderID string Carrier string Status string Version int64 Weight float64 UpdatedAt time.Time Attributes map[string]string }
ID string
OrderID string
Carrier string
Status string
Version int64
Weight float64
UpdatedAt time.Time
Attributes map[string]string
Querier interface { QueryRow(ctx context.Context, sql string, args ...any) Row Exec(ctx context.Context, sql string, args ...any) (int64, error) }
Row interface { Scan(dest ...any) error }
Store struct { pool *pool.Pool breaker *breaker.Breaker timeout time.Duration }
pool *pool.Pool
breaker *breaker.Breaker
timeout time.Duration
func New(p *pool.Pool, b *breaker.Breaker, timeout time.Duration) *Store    # New wires a store over a pool and a breaker.
    func (s *Store) withConn(ctx context.Context, fn func(context.Context, Querier) error) error    # withConn borrows a connection, runs fn through the breaker and returns the connection, marking it broken when the failure looks like a transport error.
func isTransportFailure(err error) bool    # isTransportFailure decides whether a connection should be thrown away rather than returned to the idle list.
    func (s *Store) Get(ctx context.Context, id string) (*Shipment, error)    # Get loads one shipment by primary key.
    func (s *Store) UpdateStatus(ctx context.Context, id, status string, expectedVersion int64) error    # UpdateStatus advances a shipment using optimistic concurrency.
    func (s *Store) UpsertBatch(ctx context.Context, shipments []*Shipment) (int64, error)    # UpsertBatch writes many shipments in a single statement.
    func (s *Store) Ping(ctx context.Context) error    # Ping proves the store is reachable, for the readiness probe.
```

### benchmarks/repos/tsapi/src/server.ts
```typescript
# skeleton: benchmarks/repos/tsapi/src/server.ts
const SHUTDOWN_GRACE_MS = 10_000;
const MAX_BODY_BYTES = 1_048_576;
class PayloadTooLarge extends Error
public readonly status = 413
public readonly code = "payload_too_large"
    constructor()
async function readJsonBody(req: IncomingMessage): Promise<unknown>
async function toRequestContext(req: IncomingMessage): Promise<RequestContext>
function createRequestListener(router: Router)
interface RunningServer
function start(pool: Pool, config: AppConfig = loadConfig()): RunningServer
close = async (): Promise<void> =>
```

### .prompts/architecture-review.md
```
# skeleton: .prompts/architecture-review.md
# Task: Architecture Review
## Goal
## Output
## Instructions
```

## Cold — fetch on demand via MCP `read_context`/`get_symbol`
`.github/workflows/validate-ai-config.yml`, `.prompts/bug-fix.md`, `.prompts/code-review.md`, `.prompts/test-generation.md`, `CLAUDE.md`, `benchmarks/repos/pyshop/app/api/handlers.py`, `benchmarks/repos/pyshop/app/config.py`, `benchmarks/repos/pyshop/app/main.py`, `benchmarks/repos/pyshop/app/models.py`, `benchmarks/repos/pyshop/app/orders/state_machine.py`, `benchmarks/repos/pyshop/app/payments/gateway.py`, `benchmarks/repos/pyshop/app/pricing/rules.py`, `benchmarks/repos/pyshop/app/storage/repository.py`, `benchmarks/repos/pyshop/app/workers/fulfillment.py`, `benchmarks/repos/tsapi/src/auth.ts`, `benchmarks/repos/tsapi/src/config.ts`, `benchmarks/repos/tsapi/src/db.ts`, `benchmarks/repos/tsapi/src/notifications.ts`, `benchmarks/repos/tsapi/src/rateLimiter.ts`, `benchmarks/repos/tsapi/src/router.ts`, `benchmarks/repos/tsapi/src/routes.ts`, `benchmarks/repos/tsapi/src/validation.ts`, `docs/Comparison.md`, `docs/TokenEfficiency.md`, `pyproject.toml`, `tests/__init__.py`, `tests/report_js_harness.js`, `tools/validate_ai_config.py`

## Recently changed
`.github/copilot-instructions.md`, `.github/workflows/ci.yml`, `gen-context.config.json`, `tests/test_contextiq_all.py`, `tokengraph_all.py`, `README.md`, `benchmarks/repos/gosvc/judge.json`, `benchmarks/repos/pyshop/judge.json`, `benchmarks/repos/tsapi/judge.json`, `.gitignore`

## TODO / FIXME
- .github/copilot-instructions.md:549 TODO / FIXME
- .github/copilot-instructions.md:550 TODO / FIXME
- .github/copilot-instructions.md:551 TODO / FIXME
- .github/copilot-instructions.md:552 TODO / FIXME
- .github/copilot-instructions.md:553 TODO / FIXME
- .github/copilot-instructions.md:554 TODO also add pricing for llama models.\n"
- .github/copilot-instructions.md:555 TODO line continues turn 2)
- .github/copilot-instructions.md:556 TODO add a redis client and unit tests.\n")
- .github/copilot-instructions.md:557 TODO add tests. what next?\n"
- .github/copilot-instructions.md:558 TODO write docs.\n")
<!-- tokengraph:end -->
